<div class="container portfolio-container">
  <div class="row">
  <div class="col-md-7">
    <%= image_tag portfolio_img(@portfolio_item.main_image,'main'),width: '100%' %>
    </div>

    <div class="col-md-5">
      <em><%= @portfolio_item.title %></em>
      <hr />
      <i><%=  @portfolio_item.subtitle %></i>

      <p><%=  @portfolio_item.body %></p>

      <hr />
      <h2>Technologies Used</h2>
      <% @portfolio_item.technologies.each do |t| %>
        <p><%= t.name%> </p>
      <% end %>

      <% if logged_in?(:site_admin) %>
      <div>
        <%= link_to 'Edit Item', edit_portfolio_path(@portfolio_item), class: 'btn btn-warning' %>
        <%#= link_to "Delete Item", portfolio_path(@portfolio_item), :method => "delete", class: "btn btn-danger" %>
      </div>
    <% end %>
    </div>

    
  </div>
</div>